# CardioSense redesign

The user authorizes redesign of this project alongside the portfolio. Preserve the original inference/data pipeline. `src/data/inference.ts` and `models/client_model.json` define the actual browser model; do not invent or change clinical predictions to fit a visual. The research benchmark and live Logistic Regression export are different artifacts and must be labeled accurately.

Create an approachable, readable research instrument with inputs, a real probability visualization and actual signed coefficient contributions. No fake ECG or clinical severity thresholds. This is a research demo, not a diagnosis or treatment tool. Keep controls responsive and keyboard-accessible. No repository links in the parent portfolio.

Use one intentional UI typeface. Keep the input → score relationship clear and advanced inputs available without making the default screen overwhelming. Check build, TypeScript, source-derived calculation consistency and actual browser interactions. No paid APIs, credit resets or delegation. Progress lives in `C:/Users/matth/portfolio/docs/REDESIGN-STATUS.md`.
